[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "copilot-ui"
version = "0.1.0"
dependencies = ["streamlit", "pandas"]

[tool.hatch.build.targets.wheel]
packages = ["src"]
